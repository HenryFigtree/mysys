# Basic CLI system monitor written in rust.

## Features
For now it only features CPU usage. Expected to become a full-fledged system monitor.

## Instalation
```bash
git clone https://github.com/HenryFigtree/mysys.git
cd mysys
cargo build --release
